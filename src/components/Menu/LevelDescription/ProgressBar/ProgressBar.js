import { MyComponent } from '@lib';
import './ProgressBar.scss';

export class ProgressBar extends MyComponent {
  constructor() {
    super({
      textContent: 'progress bar placeholder',
    });
  }
}
